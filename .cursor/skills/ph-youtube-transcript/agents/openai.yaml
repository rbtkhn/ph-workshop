interface:
  display_name: "PH YouTube Transcript Alias"
  short_description: "Alias for PH transcript skill"
  default_prompt: "Use $ph-youtube-transcript to route YouTube transcript work to $ph-transcript."

policy:
  allow_implicit_invocation: true
